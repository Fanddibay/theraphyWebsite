{
  "devDependencies": {
    "tailwindcss": "^3.4.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tailwindcss -i ./src/input.scss -o ./src/output.css --watch"
  },
  "dependencies": {
    "@preline/stepper": "^2.5.0",
    "preline": "^2.5.0"
  }
}
